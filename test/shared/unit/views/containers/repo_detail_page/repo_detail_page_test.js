/*
 import React from 'react';

import { expect } from 'chai';
import { shallow, mount } from 'enzyme';
import { StaticRouter } from 'react-router';

import RepoDetailPage from '../../../../../src/views/containers/pages/repo_detail_page/repo_detail_page';

describe('A suite for repo-detail page', function() {
  it('contains the correct class', function correctClass() {
    expect(
      shallow(<StaticRouter context={{}}><RepoDetailPage /></StaticRouter>).find(
        '.repo-detail-page'
      ).length
    ).to.equal(1);
  });

  it('contains spec with an expectation', function() {
    expect(
      mount(<StaticRouter context={{}}><RepoDetailPage /></StaticRouter>).find(
        '.repo-detail-page'
      ).length
    ).to.equal(1);
  });
});
*/
