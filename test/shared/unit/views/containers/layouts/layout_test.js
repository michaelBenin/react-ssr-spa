/*
import React from 'react';

import { expect } from 'chai';
import { shallow, mount } from 'enzyme';
import { StaticRouter } from 'react-router';

import Layout from '../../../../../src/views/containers/layouts/layout';

describe('A suite for layout page', function() {
  it('contains the correct class', function correctClass() {
    expect(
      shallow(<StaticRouter context={{}}><Layout /></StaticRouter>).find(
        '.layout-page'
      ).length
    ).to.equal(1);
  });

  it('contains spec with an expectation', function() {
    expect(
      mount(<StaticRouter context={{}}><Layout /></StaticRouter>).find(
        '.layout-page'
      ).length
    ).to.equal(1);
  });
});
*/
