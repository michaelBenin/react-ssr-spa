/*
import React from 'react';

import { expect } from 'chai';
import { shallow, mount } from 'enzyme';
import { StaticRouter } from 'react-router';

import SearchResultsPage from '../../../../../src/views/containers/pages/search_results_page/search_results_page';

describe('A suite for search page', function() {
  it('contains the correct class', function correctClass() {
    expect(
      shallow(
        <StaticRouter context={{}}><SearchResultsPage /></StaticRouter>
      ).find('.search')
    ).to.equal(1);
  });

  it('contains spec with an expectation', function() {
    expect(
      mount(
        <StaticRouter context={{}}><SearchResultsPage /></StaticRouter>
      ).find('.search').length
    ).to.equal(1);
  });
});
*/
