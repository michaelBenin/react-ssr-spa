// on client, this is bootstrapped to redux store in the config
// on server require it or pull from config
// in sass, just use classnames from components

module.exports = {
  example_feature: true
};
